import , { @Input } from "@angular/core";
import , { @Output } from "@angular/core";

@Component({
  selector: "app-root",
  templateUrl: "app.component.html",
  styles: ["./app.component.css"]
})
class AppComponent {
  var title: string;
  var count: number;

  constructor(name: string, age: number) {
    console.log(name);
    title = "Angular";
    count = 10;
  }

  handleClick() {
    logMessage("Hello World");
    let htmlContent = <div>
                        Welcome
                        <span>User</span>
                      </div>;
  }

  logMessage(msg: string) {
    console.log(msg);
  }

  onClick() {
    callFunction("clicked");
  }

  var list: string[] = ["item1", "item2"];
  var config: object = { theme: "dark", layout: "grid" };
}
